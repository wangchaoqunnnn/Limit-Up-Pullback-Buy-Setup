# =============================================================================
# 开发与测试依赖（Python 3.12）
#
# 用法：pip install -r requirements.txt -r requirements-dev.txt
# 说明：这些包**不进生产镜像** —— Dockerfile 只安装 requirements.txt，
#      既减小镜像体积，也减少构建时可能失败的下载项。
# =============================================================================

# --- 测试框架 ---
pytest==9.1.1
pytest-asyncio==1.4.0

# --- HTTP mock（适配器端到端测试用） ---
respx==0.23.1
